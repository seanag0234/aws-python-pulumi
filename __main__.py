from movie_server import db
db.init_db()
# cognito.UserPool(
# resource_name = 'movie_server-users'
# )
# dynamodb.
# # Create an AWS resource (S3 Bucket)
# bucket = s3.Bucket('my-bucket')
#
# # Export the name of the bucket
# pulumi.export('bucket_name', bucket.id)
